# Agent 40 implementation prompt — A1.1

Status: **NOT YET ISSUED**

Agent 00 will replace this placeholder only after A1.1 reconciliation is complete and records `SLICE READY`.

The final prompt must identify the immutable planning-branch commit containing the accepted reconciliation and require Agent 40 to implement on a separate branch/PR from current `main`.
