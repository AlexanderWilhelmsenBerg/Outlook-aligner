# Agent 50 acceptance prompt — A1.1

Status: **NOT YET ISSUED**

Agent 00 will replace this placeholder after Agent 40 has an implementation PR with an exact current head.

The final prompt must require independent exact-head review and exact-head CI verification. Agent 50 must not merge.
